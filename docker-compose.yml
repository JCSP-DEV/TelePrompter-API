version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - mysql
    networks:
      - tfg_network
    restart: always

  mysql:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: testtfg
      MYSQL_PASSWORD: root
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    networks:
      - tfg_network
    restart: always

networks:
  tfg_network:
    driver: bridge